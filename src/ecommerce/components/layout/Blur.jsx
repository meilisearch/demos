import React from "react";

const Blur = () => {
  return (
    <div
      className="styles.blur"
      role="button"
      tabIndex="-1"
      aria-label="Close cart view"
    />
  );
};

export default Blur;
